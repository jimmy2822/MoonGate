<table class="table">
    <thead>
        <tr>
            <th><%= check_box_tag("checked")%></th>
            <th>管理工具</th>
            <th>ID</th>
            <th>名稱</th>
            <th>擁有者帳號</th>
            <th></th>
        </tr>
    </thead>
    
    <tbody>
        <% @gates.each do |gate|%>
        <tr>
            <td><%= check_box_tag("checked")%></td>
            <td><%= link_to "刪除", admin_gate_path(gate) , method:"delete"  %> </td>
            <td><%= gate.id %></td>
            <td><%= gate.name %></td>
            <td><%= gate.user.email%></td>
            <td></td>
            <td></td>
        </tr>
        <% end %>
    </tbody>
</table>
<div class="center"><%= paginate(@gates)%></div>