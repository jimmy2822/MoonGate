$('#gates-list').append('<%= j render 'gates', gates: @gates %>');

console.log("append complete!")

<% if @gates.next_page %>
  $('.pagination').replaceWith('<%= j paginate(@gates) %>');
<% else %>
  $('.pagination').remove();
<% end %>

