<div class="hot-tags col-lg-8 container-fluid">
    <ul>熱門標籤：
    <% @tags.each do |tag|%>
        <li style= "display:inline; ">
            <%= link_to tag, search_tag_gates_path(search_word: tag.to_s), class:"btn-tag" %>
        </li>
    <% end %>
    </ul>
</div>

<div class="col-lg-4">
  <h4>月門讓分享 Discord 頻道更簡單！</h4>
</div>


<div class="col-lg-12"><h4><%= flash[:notice]%></h4></div>

<div class="container-fluid col-lg-12" id="gates-list">
    <% @gates.each do |gate| %>
    <div class="col-lg-4 gate-box container">
        <div class="gate-box-header">
            <div class="col-lg-2 gate-box-icon">
                <%= show_gate_icon(gate) %>
            </div>
            <div class="col-lg-10 gate-box-name">
                <%= link_to gate.name, gate_path(gate[:id]), target:"_blank" %>
            </div>
        </div>

        <div class="gate-box-body">
            <div class="col-lg-12 gate-tool-top">
                <%= link_to (content_tag :i," 加入伺服器",class:"fa fa-moon-o").html_safe, join_server_gates_path(:gate_server=>gate.server ), class:"btn-tag" %>
            </div>
            
            <div class="col-lg-12 gate-intro"><%= gate.intro %></div>
        </div>

        <div class="gate-box-tool">
            <span class="gate-tag col-lg-10"><i class="glyphicon glyphicon-tags">：</i><% search_tag_button(gate.tag_list) %></span>
            <span class="gate-like"><%= like_gate_button(gate) %></span>
        </div>
    </div>
    <% end %>
</div>

<div class="col-lg-12 center"> <%=paginate @gates%> </div>
